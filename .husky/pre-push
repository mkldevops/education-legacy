#!/usr/bin/env sh
echo "Running pre-push validations..."

# Run full test suite (essential for push)
echo "Running tests..."
make test-all

if [ $? -ne 0 ]; then
  echo "❌ Tests must pass before push!"
  exit 1
fi

echo "✅ All pre-push checks passed!"