#!/usr/bin/env sh
echo "Running pre-push validations..."

# Run tests without resetting database fixtures
echo "Running tests..."
make test

if [ $? -ne 0 ]; then
  echo "❌ Tests must pass before push!"
  exit 1
fi

echo "✅ All pre-push checks passed!"