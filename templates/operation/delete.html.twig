{% extends 'operation/common.html.twig' %}

{% trans_default_domain 'operation' %}

{% set title = 'title.delete'|trans({'%name%' : operation }) %}

{% set breadcrumbs = { ('' ~ title) : path('app_operation_delete', {id : operation.id })}|merge(breadcrumbs|default({})) %}

{% block errors %}{{ form_errors(delete_form) }}{% endblock errors %}

{% block body %}
    {% embed '_inspina/embed/delete.html.twig' %}
        {% trans_default_domain 'operation' %}
        {% set nameEntity = 'operation'|trans %}
        {% block title 'title.form.delete'|trans %}
    {% endembed %}
{% endblock %}
