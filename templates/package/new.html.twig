{% extends 'package/common.html.twig' %}

{% trans_default_domain 'package' %}

{% set title = 'package.title.new'|trans %}

{% block errors %}{{ form_errors(form) }}{% endblock errors %}

{% set breadcrumbs = { ('' ~ title) : path('package_new')}|merge(breadcrumbs|default({})) %}

{% block body %}

    {% embed '_inspina/embed/ibox.html.twig' %}
        {% trans_default_domain "package" %}
        {% block title 'form.title.new'|trans %}
        {% block config '' %}
        {% block content %}
            {% include "form.html.twig" %}
        {% endblock %}
    {% endembed %}

{% endblock %}
